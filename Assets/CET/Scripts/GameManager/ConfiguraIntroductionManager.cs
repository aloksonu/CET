using UnityEngine;

namespace CET.Scripts.GameManager
{
    public class ConfiguraIntroductionManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
